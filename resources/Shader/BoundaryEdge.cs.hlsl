#include "Boundary.hlsli"

//struct Elem {
//	float3 position;
//	float3 rotate;
//	float3 scale;
//};

//StructuredBuffer<Hole>   gHoles  : register(t0);
//RWStructuredBuffer<Elem> gOutput : register(u0);
//RWTexture2D<float4>      gMask   : register(u1);

//[numthread(128, 1, 1)]
//void main(uint3 DTid : SV_DispatchThreadID) {
//	/// 
//	int holeIndex = int(DTid.x);
//	Hole hole = gHoles[holeIndex];
	
	

//	gOutput[holeIndex] = hole;
//}